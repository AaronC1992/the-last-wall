# Gameplay Overhaul Plan

1. Replace campaign use of the generic wave picker with explicit encounter groups, while preserving generated Basic, Mixed, and Elite custom map behavior.
2. Rebalance fresh economy and tower slots, then add first clear rewards and campaign performance ratings.
3. Audit every permanent upgrade, wire dead effects, and add a small set of transformative tower specializations through shared tower bonus data.
4. Centralize tower threat geometry, keep fixed and smart targeting intentional, and correct range scaling.
5. Add save migrations, full Map Builder snapshots, Vitest regression coverage, and F2 subsystem timing.
6. Update documentation, run tests and production build, then perform browser smoke checks and record measured results.